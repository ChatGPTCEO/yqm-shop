<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yqm.common.mapper.TpSiteMapper">


    <update id="updateAllSortGal">
        update tp_site
        set sort = sort + 1
        where `sort` <![CDATA[ <= ]]>  #{currentSort}
          and user_id = #{userId}
    </update>

    <update id="top">
        update tp_site
        set sort = 1
        where id <![CDATA[ = ]]>  #{id}
          and user_id = #{userId}
    </update>

    <select id="getMaxSort" resultType="int">
        select IFNULL(max(sort), 1)
        from tp_site
        where user_id = #{userId}
          and `status` in ('effective', 'failure')
    </select>

</mapper>